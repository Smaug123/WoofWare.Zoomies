namespace WoofWare.Zoomies.Port.Test

/// Entry point for test execution
module Program =
    
    [<EntryPoint>]
    let main args =
        // NUnit will handle test discovery and execution
        // This is just a placeholder main function
        printfn "WoofWare.Zoomies.Port.Test - Bonsai F# Port Tests"
        printfn "Run with: dotnet test"
        0